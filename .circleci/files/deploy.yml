apiVersion: apps/v1
kind: Deployment
metadata:
  name: devops-deployment-${CIRCLE_WORKFLOW_ID}
  labels:
    app: devops-${CIRCLE_WORKFLOW_ID}
spec:
  replicas: 3
  selector:
    matchLabels:
      app: devops-${CIRCLE_WORKFLOW_ID}
  template:
    metadata:
      labels:
        app: devops-${CIRCLE_WORKFLOW_ID}
    spec:
      containers:
        - name: devops
          image: cosmincaloian/devops-server:${CIRCLE_WORKFLOW_ID}
          ports:
            - containerPort: 3000